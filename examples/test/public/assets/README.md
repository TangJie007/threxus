# 素材目录

```
public/assets/
├── hdri/      # 环境光照贴图 .hdr / .exr（1K 足够，2K 只在特写镜头需要）
├── models/    # 设备模型 .glb（必须已做 Draco/Meshopt + KTX2 压缩）
└── textures/  # 通用 PBR 贴图 .ktx2 / .webp
```

> **当前状态：本目录为空，项目 100% 程序化生成，离线可跑。**
> 放素材进来只是「锦上添花」，不影响运行。详见根目录 `ASSETS.md`。
